name: Cypress Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  cypress-run:
    runs-on: ubuntu-22.04
    steps:
      - name: Cypress run
        uses: cypress-io/github-action@v5
        with:
          build: npm install
          start: npx cypress run
